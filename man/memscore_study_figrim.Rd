% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark.R
\name{memscore_study_figrim}
\alias{memscore_study_figrim}
\title{Run the repeated FIGRIM study}
\usage{
memscore_study_figrim(
  figrim_root,
  protocols = c("across", "within"),
  score_types = c("corrected_hit_rate"),
  clip_models = c("ViT-B-32", "RN50"),
  clip_pretrained = "openai",
  clip_tta = "none",
  clip_tta_resize = NULL,
  pca_dims = c("none", "128", "64", "32"),
  pls_dims = character(),
  num_splits = 10L,
  train_size = 0.7,
  val_size = 0.1,
  test_size = 0.2,
  batch_size = 32L,
  device = "cpu",
  cache_dir = NULL,
  resmem_checkpoint = NULL,
  output_json = NULL,
  output_csv = NULL,
  random_state = 0L,
  ensemble_weight_step = 0.05,
  cli = getOption("memscore.cli"),
  python = getOption("memscore.python"),
  backend = getOption("memscore.backend", "auto"),
  workdir = getOption("memscore.workdir")
)
}
\arguments{
\item{figrim_root}{Path to the FIGRIM download directory.}

\item{protocols}{FIGRIM protocols to evaluate.}

\item{score_types}{FIGRIM target definitions to evaluate.}

\item{clip_models}{Character vector of frozen CLIP backbones to evaluate.}

\item{clip_pretrained}{Pretrained tag passed through to the backend.}

\item{clip_tta}{CLIP test-time augmentation: \code{none}, \code{fivecrop}, or
\code{tencrop}.}

\item{clip_tta_resize}{Optional resize target for CLIP test-time
augmentation.}

\item{pca_dims}{PCA dimensions to evaluate. Use \code{"none"} for full
embeddings.}

\item{pls_dims}{Optional PLS component counts.}

\item{num_splits}{Number of repeated stratified splits.}

\item{train_size}{Fraction assigned to the training split.}

\item{val_size}{Fraction assigned to the validation split.}

\item{test_size}{Fraction assigned to the test split.}

\item{batch_size}{Batch size for inference.}

\item{device}{Torch device string passed through to the backend.}

\item{cache_dir}{Optional cache directory for downloaded checkpoints.}

\item{resmem_checkpoint}{Optional explicit path to a ResMem checkpoint.}

\item{output_json}{Optional summary JSON path. A temporary file is created if
omitted.}

\item{output_csv}{Optional summary CSV path. A temporary file is created if
omitted.}

\item{random_state}{Random seed for challenger selection.}

\item{ensemble_weight_step}{Weight step used for validation-weighted
ensembling.}

\item{cli}{Optional explicit path to a \code{memscore} executable.}

\item{python}{Optional explicit path to a Python interpreter.}

\item{backend}{Backend mode. Use \code{"cli"} to force command-line execution or
\code{"reticulate"} to call the Python package in-process.}

\item{workdir}{Optional working directory for backend execution.}
}
\value{
A list with \code{study}, \code{output_json}, \code{output_csv}, and \code{stdout}.
}
\description{
Runs \verb{memscore study-figrim}.
}
\examples{
\dontrun{
memscore_study_figrim(
  figrim_root = "/path/to/figrim",
  clip_models = c("ViT-B-32", "RN50", "ViT-B-16"),
  clip_tta = "fivecrop"
)
}
}
