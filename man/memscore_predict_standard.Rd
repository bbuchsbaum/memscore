% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/standard.R
\name{memscore_predict_standard}
\alias{memscore_predict_standard}
\title{Score image memorability with the memscore standard model}
\usage{
memscore_predict_standard(
  paths,
  model = NULL,
  recursive = FALSE,
  batch_size = 32L,
  device = "cpu",
  cache_dir = NULL,
  output = NULL,
  clip_scores = TRUE,
  cli = getOption("memscore.cli"),
  python = getOption("memscore.python"),
  backend = getOption("memscore.backend", "auto"),
  workdir = getOption("memscore.workdir")
)
}
\arguments{
\item{paths}{Character vector of image files or directories.}

\item{model}{Path to a saved standard scorer artifact. If omitted, the Python
backend uses a packaged artifact when one is installed.}

\item{recursive}{Recurse into subdirectories when a path is a directory.}

\item{batch_size}{Batch size for inference.}

\item{device}{Torch device string passed through to the backend.}

\item{cache_dir}{Optional cache directory for downloaded checkpoints.}

\item{output}{Optional CSV path for backend-generated predictions.}

\item{clip_scores}{Clamp predicted scores to the \verb{[0, 1]} memorability range.}

\item{cli}{Optional explicit path to a \code{memscore} executable.}

\item{python}{Optional explicit path to a Python interpreter.}

\item{backend}{Backend mode. Use \code{"cli"} to force command-line execution or
\code{"reticulate"} to call the Python package in-process.}

\item{workdir}{Optional working directory for backend execution.}
}
\value{
A data frame with \code{id}, \code{image_path}, and \code{memorability}.
}
\description{
Scores images with a saved \code{memscore} standard scorer artifact: frozen CLIP
backbones, ridge heads, and a mean ensemble. Use \code{\link[=memscore_train_standard_scorer]{memscore_train_standard_scorer()}}
to create the artifact from labeled manifests. ResMem remains available via
\code{\link[=memscore_predict]{memscore_predict()}}.
}
\examples{
\dontrun{
memscore_predict_standard("image.jpg", model = "memscore_standard.pkl")
}
}
